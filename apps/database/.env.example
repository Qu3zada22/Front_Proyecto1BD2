# Desarrollo local (Docker)
MONGODB_URI=mongodb://localhost:27017/fastpochi?directConnection=true

# Atlas (opcional — si el nombre de BD está en la URI, MONGO_DB se ignora)
# MONGODB_URI=mongodb+srv://usuario:password@cluster.mongodb.net/fastpochi
# MONGO_DB=fastpochi   ← solo necesario si la URI no incluye el nombre de BD
