# ============================================================
# FastPochi Client — Variables de entorno
# Copia como .env.local para desarrollo local
# ============================================================

# URL base de la API — dejar en /api para usar el proxy de Vite (dev) o nginx (prod)
# Para apuntar a una API remota: VITE_API_URL=https://api.midominio.com/api
VITE_API_URL=/api
