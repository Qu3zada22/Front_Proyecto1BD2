# ROADMAP Auditoría #12 — FastPochi Backend

**Fecha:** 2026-03-10
**Estado previo:** 212/212 tests ✓ | notablescan: ON | todos los bugs #11 cerrados

---

## Tareas pendientes

### BUG-01 — `$toDouble` → `$toDecimal` en `ingresosPorDia` y `platillosMasVendidos`

**Archivo:** `apps/api/src/reportes/reportes.service.ts`

**Cambios:**

1. `ingresosPorDia` (L133, L135):

   ```typescript
   total_ingresos: { $sum: { $toDecimal: '$total' } },
   ticket_promedio: { $avg: { $toDecimal: '$total' } },
   ```

2. `platillosMasVendidos` (L102):
   ```typescript
   ingresos: { $sum: { $toDecimal: '$items.subtotal' } },
   ```

**Tests a actualizar:** `apps/api/src/reportes/reportes.service.spec.ts`

- `describe('ingresosPorDia')`: verificar que el pipeline usa `$toDecimal` en `total_ingresos` y `ticket_promedio`
- `describe('platillosMasVendidos')`: verificar que el pipeline usa `$toDecimal` en `ingresos`

---

### BUG-02 — `cancelarRestaurante` `$push` sin `$slice`

**Archivo:** `apps/api/src/restaurantes/restaurantes.service.ts` (L113)

**Cambio:**

```typescript
// De:
{ $set: { estado: 'cancelado' }, $push: { historial_estados: histEntry } }

// A:
{ $set: { estado: 'cancelado' }, $push: { historial_estados: { $each: [histEntry], $slice: -5 } } }
```

**Tests a actualizar:** `apps/api/src/restaurantes/restaurantes.service.spec.ts`

- Verificar que el update de ordenes usa `$push.historial_estados.$each[0]` y `$slice: -5`

---

### OBS-01 — Swagger description de `topRestaurantes`

**Archivo:** `apps/api/src/reportes/reportes.controller.ts` (L39)

**Cambio:**

```typescript
description: "Aggregation compleja: parte de resenas (fuente de verdad) → $group avg/count → $match(≥5 reseñas) → $sort/$limit → $lookup restaurantes → $project.";
```

---

## Orden de ejecución recomendado

1. BUG-01: cambiar `$toDouble` → `$toDecimal` en service + actualizar specs
2. BUG-02: añadir `$each+$slice:-5` en cancelarRestaurante + actualizar spec
3. OBS-01: actualizar Swagger description (1 línea)
4. Ejecutar `npm test` — esperar 212/212 ✓
5. Commit: `fix: $toDecimal en ingresosPorDia/platillosMasVendidos; $slice en cancelarRestaurante`

---

## Estado objetivo

- 212/212 tests ✓ (sin cambiar el número — solo actualizaciones de specs existentes)
- `$toDecimal` consistente en los 4 métodos monetarios del ReportesService
- `historial_estados` acotado en máximo 5 entradas en TODOS los paths (updateStatus + cancelarRestaurante)
- Swagger description de `topRestaurantes` actualizada
