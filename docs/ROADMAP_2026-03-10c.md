# Roadmap de Fixes — Audit #5 (2026-03-10)
**Tests actuales:** 191 passed / 11 suites

---

## FIX-01 — Deduplicar itemIds en el check de disponibilidad (crear orden)

**Prioridad:** LOW
**Archivo:** `apps/api/src/ordenes/ordenes.service.ts`

**Cambio:**
```typescript
// ANTES:
const itemIds = itemsMapped.map(i => i.item_id);
const disponibles = await this.menuItemModel
    .find({ _id: { $in: itemIds }, disponible: true }, { _id: 1 }, { session })
    .lean();
if (disponibles.length !== itemIds.length) { ... }

// DESPUÉS:
const itemIds = itemsMapped.map(i => i.item_id);
const uniqueItemIds = [...new Set(itemIds.map(id => id.toHexString()))]
    .map(hex => new Types.ObjectId(hex));
const disponibles = await this.menuItemModel
    .find({ _id: { $in: uniqueItemIds }, disponible: true }, { _id: 1 }, { session })
    .lean();
if (disponibles.length !== uniqueItemIds.length) { ... }
```

**Actualización de spec (`ordenes.service.spec.ts`):**
- Actualizar el test "should check disponible:true..." para verificar que se pasan IDs únicos
- Añadir test: pedir el mismo item dos veces NO lanza error si está disponible

---

## Checklist rúbrica final

| Criterio                                              | Estado |
|-------------------------------------------------------|--------|
| Transacción Crear Orden (ACID + check disponible)     | ✅ (BUG-01 es edge case menor) |
| Transacción Cancelar Restaurante (ACID)               | ✅     |
| Top restaurantes con reseñas activas (activa:true)    | ✅     |
| Best sellers ($unwind + $group + $toDouble Decimal128) | ✅     |
| Revenue by day / by restaurant+month                  | ✅     |
| Top spenders ($lookup + exclusión password)           | ✅     |
| Desnorm en tiempo real (calificacion_prom, tiene_resena, veces_ordenado) | ✅ |
| GridFS upload/download/delete                         | ✅     |
| Índices: unique, compound ESR, multikey, 2dsphere, text | ✅   |
| Seed 50k docs + reseñas + ratings                     | ✅     |
| Tests: 191+ passed                                    | ✅     |
