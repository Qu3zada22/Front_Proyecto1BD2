# Roadmap — Post Auditoría #19

**Fecha:** 2026-03-10
**Estado actual:** 241 tests passing | 1 bug + 4 observaciones pendientes (sin cambios desde Audit #18)

---

## Prioridad Alta (afecta demo)

### 1. BUG-01: `verify.js` — `.toFixed(2)` sobre Decimal128 → crash

**Archivo:** `apps/database/verify.js` (líneas 142, 164)

- [ ] Línea 142 — reemplazar `r.total_revenue.toFixed(2)` y `r.ticket_prom.toFixed(2)`:
  ```javascript
  const rev = parseFloat(r.total_revenue.toString());
  const tick = parseFloat(r.ticket_prom.toString());
  console.log(
    `  ${r.restaurante.padEnd(28)} Q${String(rev.toFixed(2)).padStart(10)}  ticket_prom: Q${tick.toFixed(2)}  (${r.num_ordenes} órdenes)`,
  );
  ```
- [ ] Línea 164 — reemplazar `r.total_gastado.toFixed(2)`:
  ```javascript
  const gastado = parseFloat(r.total_gastado.toString());
  console.log(
    `  ${r.cliente.padEnd(28)} ${r.total_ordenes} órdenes  Q${gastado.toFixed(2)} gastado`,
  );
  ```
- [ ] Verificar que `npm run verify` completa sin errores

**Estimación:** ~6 líneas

---

## Prioridad Media

### 2. OBS-02: `menu-items.service.ts` — Verificar `activo: true` al crear item

**Archivos:** `apps/api/src/menu-items/menu-items.service.ts`, `menu-items.service.spec.ts`

- [ ] `menu-items.service.ts` línea 17: cambiar a `countDocuments({ _id: dto.restaurante_id, activo: true })`
- [ ] Actualizar mensaje: `'El restaurante referenciado no existe'` → `'El restaurante referenciado no existe o está inactivo'`
- [ ] `menu-items.service.spec.ts` línea 98: actualizar `rejects.toThrow()` con nuevo mensaje
- [ ] Agregar test: restaurante inactivo → BadRequestException con nuevo mensaje

**Estimación:** ~3 líneas de código + ~8 de tests

### 3. OBS-03: `reportes.service.ts` — Renombrar `_id` a `categoria`

**Archivos:** `apps/api/src/reportes/reportes.service.ts`, `reportes.service.spec.ts`

- [ ] `reportes.service.ts` líneas 155-161: agregar `{ $project: { categoria: '$_id', total: 1, _id: 0 } }` como 4to stage
- [ ] `reportes.service.spec.ts` línea 542: agregar verificación `expect(pipeline[3]).toEqual({ $project: { categoria: '$_id', total: 1, _id: 0 } })`
- [ ] (Opcional) Actualizar expected value en el test a `{ categoria: 'italiana', total: 5 }` format

**Estimación:** ~2 líneas de código + ~5 de tests

---

## Prioridad Baja

### 4. OBS-01: `create-orden.dto.ts` — `precio`/`nombre` opcionales

**Archivo:** `apps/api/src/ordenes/dto/create-orden.dto.ts`

- [ ] Cambiar `@ApiProperty` → `@ApiPropertyOptional` en `nombre` con descripción "Ignorado — el nombre real se lee de la BD"
- [ ] Cambiar `@ApiProperty` → `@ApiPropertyOptional` en `precio` con descripción "Ignorado — el precio real se lee de la BD"
- [ ] Agregar `@IsOptional()` antes de `@IsString()` en `nombre`
- [ ] Agregar `@IsOptional()` antes de `@IsNumber()` en `precio`
- [ ] Hacer `nombre?: string` y `precio?: number` (con `?`)

**Estimación:** ~6 líneas

### 5. OBS-04: `usuarios.controller.ts` — NaN guard en `parseInt`

**Archivo:** `apps/api/src/usuarios/usuarios.controller.ts`

- [ ] Línea 34: cambiar `parseInt(skip, 10)` → `parseInt(skip, 10) || 0`
- [ ] Línea 35: cambiar `parseInt(limit, 10)` → `parseInt(limit, 10) || 50`

**Estimación:** ~2 líneas

---

## Orden de Ejecución Recomendado

```
1. BUG-01 — verify.js Decimal128 crash (crítico para demo)
2. OBS-02 — menu-items activo:true + tests
3. OBS-03 — restaurantesPorCategoria $project + tests
4. OBS-01 — precio/nombre opcionales en DTO (cosmético)
5. OBS-04 — parseInt NaN guard (cosmético)
6. npx jest --no-cache → verificar ≥244 tests (241 existentes + 3 nuevos de OBS-02/03)
7. npm run verify → verificar que completa sin errores
```

---

## Métricas Esperadas Post-Fix

| Métrica                | Antes    | Después (esperado) |
| ---------------------- | -------- | ------------------ |
| Tests                  | 241      | ~244               |
| Suites                 | 11       | 11                 |
| Bugs abiertos          | 1        | 0                  |
| Observaciones abiertas | 4        | 0                  |
| `npm run verify`       | ❌ crash | ✅ completa        |

---

## Checklist Final Pre-Entrega

- [ ] `POST /api/seed` completa sin errores (50k órdenes)
- [ ] `POST /api/orders` con datos seeded → total correcto (no null/NaN)
- [ ] `GET /api/reports/revenue/by-day` → devuelve ingresos
- [ ] `GET /api/reports/restaurants/top-rated` → devuelve restaurantes
- [ ] `GET /api/reports/menu-items/best-sellers` → devuelve items
- [ ] `POST /api/files/upload` + `GET /api/files/:id` → funciona
- [ ] `DELETE /api/seed` limpia toda la DB
- [ ] `npx jest --no-cache` → 0 failures
- [ ] `npm run verify` → completa sin errores (después de BUG-01 fix)
- [ ] API responde en `http://localhost:3000/api`
- [ ] Swagger disponible en `http://localhost:3000/docs`
- [ ] Al menos 4 tipos de índices visibles en explain() output de verify.js
