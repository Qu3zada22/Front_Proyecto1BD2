# Roadmap de Fixes — Audit #7 (2026-03-10e)
**Tests actuales:** 197 passed / 11 suites

---

## FIX-01 — Corregir fecha por defecto en `ingresosPorDia`

**Prioridad:** LOW (visible en demo)
**Archivo:** `apps/api/src/reportes/reportes.controller.ts`

**Cambio:**
```typescript
// ANTES:
@ApiQuery({ name: 'desde', ..., example: '2024-01-01', description: 'Fecha inicio ISO (default: 2024-01-01, datos entre 2023-2025)' })
ingresosPorDia(@Query('desde') desde: string, @Query('hasta') hasta: string) {
    const start = desde ?? '2024-01-01';
    const end = hasta ?? '2025-12-31';
    ...
}

// DESPUÉS:
@ApiQuery({ name: 'desde', ..., example: '2023-01-01', description: 'Fecha inicio ISO (default: 2023-01-01, datos entre 2023-2025)' })
ingresosPorDia(@Query('desde') desde: string, @Query('hasta') hasta: string) {
    const start = desde ?? '2023-01-01';
    const end = hasta ?? '2025-12-31';
    ...
}
```

**No requiere cambios en spec** — el servicio no tiene default, solo el controlador.

---

## Checklist rúbrica final (post Audit #7)

| Criterio                                              | Estado |
|-------------------------------------------------------|--------|
| Transacción Crear Orden (ACID + check disponible + dedup) | ✅  |
| Transacción Cancelar Restaurante (ACID)               | ✅     |
| Top restaurantes con reseñas activas (activa:true)    | ✅     |
| Best sellers ($unwind + $group + $toDouble Decimal128) | ✅    |
| Revenue by day (rango correcto 2023-2025)             | ⚠️ FIX-01 pendiente |
| Revenue by restaurant/month (zero-padded)             | ✅     |
| Top spenders ($lookup + exclusión password)           | ✅     |
| Desnorm (calificacion_prom, tiene_resena, veces_ordenado) | ✅  |
| GridFS upload/download/delete                         | ✅     |
| Índices: unique, compound ESR, multikey, 2dsphere, text | ✅   |
| Seed 50k docs + reseñas + ratings                     | ✅     |
| Tests: 197+ passed                                    | ✅     |
