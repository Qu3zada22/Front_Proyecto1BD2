# Roadmap de Fixes — Audit #4 (2026-03-10)
**Tests actuales:** 189 passed / 11 suites

---

## FIX-01 — Filtrar `activa: true` en $lookup de `topRestaurantes`

**Archivo:** `apps/api/src/reportes/reportes.service.ts`
**Prioridad:** MEDIUM

Cambiar el `$lookup` plano por uno con `pipeline` que filtre `activa: true`:

```typescript
// ANTES:
{ $lookup: {
    from: 'resenas',
    localField: '_id',
    foreignField: 'restaurante_id',
    as: 'resenas',
} }

// DESPUÉS:
{ $lookup: {
    from: 'resenas',
    localField: '_id',
    foreignField: 'restaurante_id',
    pipeline: [{ $match: { activa: true } }],
    as: 'resenas',
} }
```

Impacto en tests: verificar si `reportes.service.spec.ts` valida el contenido exacto del pipeline de `topRestaurantes` y actualizar si es necesario.

---

## FIX-02 — Validar `disponible: true` en `crear orden`

**Archivo:** `apps/api/src/ordenes/ordenes.service.ts`
**Prioridad:** MEDIUM

Añadir dentro de la transacción (antes del `create`), un query que verifique disponibilidad:

```typescript
// DENTRO del bloque try, después de obtener itemsMapped y ANTES del create:
const itemIds = itemsMapped.map(i => i.item_id);
const disponibles = await this.menuItemModel
    .find({ _id: { $in: itemIds }, disponible: true }, { _id: 1 }, { session })
    .lean();
if (disponibles.length !== itemIds.length) {
    throw new BadRequestException('Uno o más platillos no están disponibles');
}
```

**Actualización de spec (`ordenes.service.spec.ts`):**
- Añadir `find: jest.fn()` a `mockMenuItemModel`
- En `beforeEach` default: mockear `find` para devolver todos los items del baseDto (check pasa)
- Añadir nuevo `describe('disponibilidad')` con test: cuando find devuelve menos items → BadRequestException

---

## Orden de ejecución

1. FIX-01: editar `reportes.service.ts` (1 línea)
2. Correr tests — si falla, ajustar spec de `topRestaurantes`
3. FIX-02: editar `ordenes.service.ts` + actualizar `ordenes.service.spec.ts`
4. Correr tests completos
5. Commit

---

## Checklist rúbrica post-fixes

| Criterio                                         | Estado esperado |
|--------------------------------------------------|-----------------|
| Transacción Crear Orden con check disponibilidad | ✅ post FIX-02  |
| Transacción Cancelar Restaurante (ACID)          | ✅              |
| Top restaurantes con reseñas activas solamente   | ✅ post FIX-01  |
| Best sellers ($unwind + $group)                  | ✅              |
| Revenue by day / by restaurant+month             | ✅              |
| Top spenders                                     | ✅              |
| Desnorm campos actualizados en tiempo real       | ✅              |
| Todas las aggregations con $toDouble Decimal128  | ✅              |
| GridFS upload/download/delete                    | ✅              |
| Tests: 190+ passed                               | ✅ post fixes   |
