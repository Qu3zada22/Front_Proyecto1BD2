# ROADMAP Auditoría #10 — FastPochi Backend
**Fecha:** 2026-03-10
**Basado en:** AUDIT_REPORT_2026-03-10h.md

---

## Fixes a aplicar (ordenados por prioridad)

### FIX-01 [ALTA] — Añadir tests para `usuariosConMayorGasto`

**Archivo:** `apps/api/src/reportes/reportes.service.spec.ts`

Añadir `describe('usuariosConMayorGasto')` al final del suite con los siguientes tests:

1. `should match only entregado orders` — verifica `pipeline[0].$match.estado === 'entregado'`
2. `should group by usuario_id with $sum $toDouble on total` — verifica `$group._id === '$usuario_id'` y `total_gastado.$sum.$toDouble === '$total'`
3. `should sort by total_gastado descending` — verifica `$sort.total_gastado === -1`
4. `should apply limit argument` — verifica `$limit === 5` cuando se llama con `limit=5`
5. `should use default limit of 10` — verifica `$limit === 10` sin argumento
6. `should include $lookup to usuarios collection` — verifica `$lookup.from === 'usuarios'` y `$lookup.localField === '_id'`

**Impacto:** +6 tests → 203/203

---

### FIX-02 [MEDIA] — Exponer `actor_id` y `nota` en `updateStatus`

**Archivos:**
- `apps/api/src/ordenes/ordenes.controller.ts` — añadir `@Body('actor_id')` y `@Body('nota')`
- Actualizar `@ApiBody` para documentar los campos opcionales

**Cambio en controller:**
```typescript
@ApiBody({ schema: { example: { estado: 'en_proceso', actor_id: '64a...', nota: 'Cocina confirmó' } } })
updateStatus(
    @Param('id', ParseMongoIdPipe) id: string,
    @Body('estado') estado: string,
    @Body('actor_id') actorId?: string,
    @Body('nota') nota?: string,
) {
    return this.ordenesService.updateStatus(id, estado, actorId, nota);
}
```

**Impacto:** Sin cambio en tests (el service ya tiene coverage de actor_id/nota vía `ordenes.service.spec.ts`)

---

### FIX-03 [MEDIA] — Añadir filtro `disponible` a `menu-items.findAll`

**Archivos:**
- `apps/api/src/menu-items/menu-items.service.ts` — añadir `disponible?: boolean` al query type, default `true`
- `apps/api/src/menu-items/menu-items.controller.ts` — añadir `@ApiQuery` para `disponible`
- `apps/api/src/menu-items/menu-items.service.spec.ts` — añadir test que verifica `{ disponible: true }` en el filtro por defecto

**Cambio en service:**
```typescript
async findAll(query: {
    restaurante_id?: string;
    categoria?: string;
    etiqueta?: string;
    disponible?: boolean;
    skip?: number;
    limit?: number;
}): Promise<MenuItemDocument[]> {
    const filter: any = {};
    // Si no se especifica, mostrar solo disponibles (comportamiento por defecto)
    filter.disponible = query.disponible !== undefined ? query.disponible : true;
    if (query.restaurante_id) filter.restaurante_id = new Types.ObjectId(query.restaurante_id);
    if (query.categoria) filter.categoria = query.categoria;
    if (query.etiqueta) filter.etiquetas = query.etiqueta;
    ...
```

**Cambio en controller:**
```typescript
@ApiQuery({ name: 'disponible', required: false, type: Boolean, description: 'Filtrar por disponibilidad (default: true)' })
findAll(
    @Query() pagination: PaginationDto,
    @Query('restaurante_id') restauranteId?: string,
    @Query('categoria') categoria?: string,
    @Query('etiqueta') etiqueta?: string,
    @Query('disponible') disponible?: string,
) {
    const disponibleBool = disponible === undefined ? undefined : disponible === 'true';
    return this.menuItemsService.findAll({ ...pagination, restaurante_id: restauranteId, categoria, etiqueta, disponible: disponibleBool });
}
```

**Nuevo test en spec:**
```typescript
it('should filter by disponible:true by default when not specified', async () => {
    const query = createMockQuery([]);
    mockModel.find.mockReturnValue(query);
    await service.findAll({});
    const callArg = mockModel.find.mock.calls[0][0];
    expect(callArg.disponible).toBe(true);
});
```

**Impacto:** +1 test → 204/204

---

### OBS-01 [OPCIONAL] — Índice `{estado:1, fecha_creacion:-1}` para `ingresosPorDia`

**Archivos:**
- `apps/api/src/ordenes/schemas/orden.schema.ts`
- `apps/database/operations/indexes.js`

**Cambio:**
```typescript
// En orden.schema.ts — añadir después de los índices existentes:
OrdenSchema.index({ estado: 1, fecha_creacion: -1 }, { name: 'idx_ordenes_estado_fecha' });
```
```javascript
// En indexes.js — createOrderIndexes():
col.createIndex({ estado: 1, fecha_creacion: -1 }, { name: 'idx_ordenes_estado_fecha' }),
```

**Impacto:** IXSCAN completo para `ingresosPorDia` (cubre estado+fecha, elimina FETCH stage)

---

## Orden de ejecución recomendado

```
1. FIX-01 (tests usuariosConMayorGasto) — solo edición de spec, sin riesgo
2. FIX-03 (disponible filter en menu-items) — service + controller + spec
3. FIX-02 (actor_id/nota en updateStatus) — solo controller, sin cambio de spec
4. OBS-01 (índice opcional) — solo si hay tiempo
```

## Verificación final

```bash
cd apps/api && npx jest --passWithNoTests
# Target: 204/204 tests (197 + 6 FIX-01 + 1 FIX-03)
```
