# Roadmap — Post Auditoría #16

**Fecha:** 2026-03-09  
**Estado actual:** 235 tests passing (11 suites) | 2 bugs + 3 observaciones pendientes

---

## Prioridad Alta

### 1. BUG-01: Recalcular precios desde BD en `ordenes.service.create`

**Archivos:** `ordenes.service.ts`, `ordenes.service.spec.ts`

- [ ] Cambiar proyección en la query de disponibilidad: `{ _id: 1 }` → `{ _id: 1, nombre: 1, precio: 1 }`
- [ ] Construir mapa `dbMap` con los items leídos de BD
- [ ] Recalcular `itemsMapped` usando `dbItem.nombre` y `dbItem.precio` de la BD (no del DTO)
- [ ] Recalcular `total` con los precios reales de la BD
- [ ] Actualizar tests: mock de `menuItemModel.find` debe devolver objetos con `nombre` y `precio`
- [ ] Agregar test que verifica que el precio del DTO se ignora y se usa el de BD

**Estimación:** ~30 líneas de código + ~20 líneas de tests

---

## Prioridad Media

### 2. BUG-02: Agregar validaciones de longitud en `CreateResenaDto`

**Archivos:** `create-resena.dto.ts`

- [ ] Agregar `@MaxLength(100)` al campo `titulo`
- [ ] Agregar `@MinLength(10)` al campo `comentario`
- [ ] Importar `MaxLength`, `MinLength` de class-validator

**Estimación:** ~3 líneas de cambio

### 3. OBS-01: Validar FK `usuario_id` y `restaurante_id` en `ordenes.service.create`

**Archivos:** `ordenes.service.ts`, `ordenes.module.ts`, `ordenes.service.spec.ts`

- [ ] Importar e inyectar `usuarioModel` y `restauranteModel` en `OrdenesService`
- [ ] Registrar `Usuario` y `Restaurante` schemas en `OrdenesModule`
- [ ] Agregar `countDocuments` antes de la transacción para usuario_id y restaurante_id
- [ ] Actualizar tests: agregar mocks para `usuarioModel` y `restauranteModel`
- [ ] Agregar tests para FK validation failure

**Estimación:** ~20 líneas de código + ~30 líneas de tests

---

## Prioridad Baja

### 4. OBS-02: Cambiar `@IsNumber()` por `@IsInt()` en `CreateResenaDto`

**Archivos:** `create-resena.dto.ts`

- [ ] Cambiar `@IsNumber()` por `@IsInt()` en el campo `calificacion`
- [ ] Importar `IsInt` de class-validator (reemplazar `IsNumber` si no se usa en otro lugar)

**Estimación:** ~2 líneas de cambio

### 5. OBS-03: Agregar `$toDecimal` en `pipelines.js` (verify)

**Archivos:** `apps/database/operations/pipelines.js`

- [ ] Cambiar `$sum: '$total'` → `$sum: { $toDecimal: '$total' }` en `revenueByRestaurant`
- [ ] Cambiar `$avg: '$total'` → `$avg: { $toDecimal: '$total' }` en `revenueByRestaurant`
- [ ] Opcionalmente: revisar `clientesMasActivos` que también usa `$sum: '$total'` sin `$toDecimal`

**Estimación:** ~4 líneas de cambio

---

## Orden de Ejecución Recomendado

1. **BUG-01** — Recalcular precios (alta prioridad, violación explícita del PDF)
2. **OBS-01** — FK validation en ordenes (se puede combinar con BUG-01 ya que ambos tocan `ordenes.service.ts`)
3. **BUG-02 + OBS-02** — Validaciones del DTO de reseñas (se pueden hacer juntos en `create-resena.dto.ts`)
4. **OBS-03** — Fix en verify.js (independiente, bajo riesgo)
5. **Correr tests** — `npx jest --no-cache` para verificar que todo pasa

---

## Métricas Esperadas Post-Fix

| Métrica              | Antes  | Después (esperado) |
| -------------------- | ------ | ------------------- |
| Tests                | 235    | ~244                |
| Suites               | 11     | 11                  |
| Bugs abiertos        | 2      | 0                   |
| Observaciones abiertas| 3     | 0                   |
| FK validation coverage| 4/5 servicios | 5/5 servicios |
| PDF spec compliance  | 99%    | 100%                |
