# Roadmap de Correcciones — FastPochi Backend
## Fecha: 2026-03-09 | Deadline: 2026-03-10 18:59

---

## Prioridad ALTA — Corregir antes de entregar

### [FIX-01] Actualizar `calificacion_prom` y `total_resenas` al crear reseña (BUG-01)
- **Archivo**: `apps/api/src/resenas/resenas.service.ts`
- **Cambio**: Después de `resenaModel.create(data)`, ejecutar update en el restaurante:
  ```typescript
  // Recalcular calificacion_prom y total_resenas desde la colección resenas
  await restauranteModel.findByIdAndUpdate(data.restaurante_id, [
      { $set: {
          total_resenas: { /* ... */ },
          calificacion_prom: { /* ... */ }
      }}
  ]);
  ```
  Mejor: usar `$inc` para total_resenas y aggregation pipeline para recalcular avg.
- **Razón**: Diseño doc: "desnormalizados, actualizados en cada reseña." API no lo hace.
- **Tests**: Añadir test que verifique que restauranteModel recibe updateOne tras create.

### [FIX-02] Marcar `tiene_resena: true` en la orden al crear reseña (BUG-02)
- **Archivo**: `apps/api/src/resenas/resenas.service.ts`
- **Cambio**: Si `data.orden_id` está presente, hacer:
  ```typescript
  await ordenModel.findByIdAndUpdate(data.orden_id, { $set: { tiene_resena: true } });
  ```
- **Razón**: Diseño doc: "DESNORM. Evita $lookup. Sinc al crear reseña."
- **Tests**: Añadir test para el update de tiene_resena.

### [FIX-03] Mapear items correctamente al crear órdenes (BUG-03)
- **Archivo**: `apps/api/src/ordenes/ordenes.service.ts`
- **Cambio**: Antes de `create()`, mapear items del DTO:
  ```typescript
  const itemsMapped = dto.items.map(i => ({
      item_id: new Types.ObjectId(i.menu_item_id),
      menu_item_id: new Types.ObjectId(i.menu_item_id),
      nombre: i.nombre,
      precio_unitario: i.precio,
      precio: i.precio,
      cantidad: i.cantidad,
      subtotal: i.precio * i.cantidad,
      ...(i.notas && { notas: i.notas }),
  }));
  ```
- **Razón**: Aggregation `platillosMasVendidos` usa `$items.item_id` y `$items.subtotal`.
- **Tests**: Actualizar spec para verificar que el doc creado tiene `item_id` y `subtotal`.

### [FIX-04] Limpiar GridFS en `ingest.js` (BUG-04)
- **Archivo**: `apps/database/ingest.js`
- **Cambio**: Añadir al inicio:
  ```javascript
  const { GridFSBucket } = await import('mongodb')
  const bucket = new GridFSBucket(db, { bucketName: 'media' })
  await bucket.drop().catch(() => {})
  console.log('  dropped: media (GridFS)')
  ```
- **Razón**: 32 archivos huérfanos por cada seed adicional.

### [FIX-05] Limpiar GridFS en `clearAll()` (BUG-05)
- **Archivo**: `apps/api/src/seed/seed.service.ts`
- **Cambio**: Inyectar `@InjectConnection()` si no está, y añadir:
  ```typescript
  const bucket = new GridFSBucket(this.connection.db as any, { bucketName: 'media' });
  await bucket.drop().catch(() => {});
  ```
- **Razón**: `DELETE /api/seed` deja GridFS con datos huérfanos.

### [FIX-06] Añadir aggregations faltantes del diseño (BUG-06)
- **Archivo**: `apps/api/src/reportes/reportes.service.ts` y `reportes.controller.ts`
- **Cambio**: Añadir:
  1. `ingresosPorRestaurantePorMes()` — pipeline: `$match entregado → $group por restaurante_id+año+mes → $sort → $lookup restaurante`
  2. `usuariosConMayorGasto(limit)` — pipeline: `$match entregado → $group por usuario_id, $sum total → $sort → $lookup usuario (sin password)`
- **Endpoints**:
  - `GET /api/reports/revenue/by-restaurant-month`
  - `GET /api/reports/users/top-spenders`
- **Tests**: Añadir specs para ambos pipelines.

---

## Prioridad MEDIA

### [CLEAN-07] `timestamps: false` en `usuarios`, `restaurantes`, `ordenes` (BUG-07)
- **Archivos**: 3 schemas
- **Cambio**: `@Schema({ timestamps: false, ... })`
- **Razón**: Consistencia con datos del seed. Los campos de fecha ya están definidos explícitamente.

---

## Prioridad BAJA

### [CLEAN-08] Remover `confirmado` del enum de estado (BUG-08)
- **Archivo**: `ordenes/schemas/orden.schema.ts`, `ordenes.service.ts`
- **Cambio**: Eliminar `'confirmado'` del tipo `EstadoOrden` y del enum del schema.
- **Razón**: No está en el diseño. 0 docs lo usan.

---

## Orden de ejecución recomendado

```
1. FIX-03 (item mapping en crear orden)         — 15 min
2. FIX-01 (actualizar calificacion_prom en reseña) — 15 min
3. FIX-02 (actualizar tiene_resena en orden)    — 10 min
4. FIX-04 (ingest.js limpia GridFS)             — 5 min
5. FIX-05 (clearAll limpia GridFS)              — 10 min
6. FIX-06 (aggregations faltantes)             — 20 min
7. Correr seed nuevo para verificar
8. Correr tests
9. CLEAN-07 (timestamps: false en 3 schemas)   — 10 min
10. CLEAN-08 (remover confirmado)               — 5 min
11. git commit
```

---

## Checklist rúbrica actualizada

- [x] Documentos embedded y referenced
- [x] CRUD completo (create, read, update, delete uno/varios)
- [x] Read con filtros, proyección, sort, skip, limit
- [x] $lookup multi-colección
- [x] GridFS upload/download
- [x] 50k+ documentos (ordenes)
- [x] Aggregation simple (count, distinct)
- [x] Aggregation compleja ($lookup pipeline)
- [x] Arrays: $push, $pull, $addToSet
- [x] Índices: simple, único, compuesto ESR, multikey, 2dsphere, text
- [x] BulkWrite (EXTRA)
- [x] Frontend React (EXTRA)
- [ ] calificacion_prom actualizado al crear reseña → FIX-01
- [ ] tiene_resena actualizado al crear reseña → FIX-02
- [ ] Items de orden con item_id + subtotal → FIX-03
- [ ] GridFS limpia al seed → FIX-04/05
- [ ] ingresosPorRestaurantePorMes → FIX-06
- [ ] usuariosConMayorGasto → FIX-06
