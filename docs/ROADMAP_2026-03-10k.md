# ROADMAP Auditoría #13 — FastPochi Backend

**Fecha:** 2026-03-10
**Estado previo:** 212/212 tests ✓ | notablescan: ON | todos los bugs #12 cerrados

---

## Tareas pendientes

### BUG-01 — `resenas.create()` envolver en transacción ACID

**Archivo:** `apps/api/src/resenas/resenas.service.ts`

**Cambios:**

1. Inyectar `@InjectConnection() private connection: Connection` en el constructor
2. Actualizar `resenas.module.ts` si es necesario (Connection ya debería estar disponible vía MongooseModule)
3. Envolver las 3 operaciones de `create()` en sesión+transacción:

```typescript
async create(data: any): Promise<ResenaDocument> {
    const session = await this.connection.startSession();
    session.startTransaction();
    try {
        const [resena] = await this.resenaModel.create([data], { session });

        if (data.restaurante_id) {
            const [stats] = await this.resenaModel.aggregate([
                { $match: { restaurante_id: new Types.ObjectId(data.restaurante_id), activa: true } },
                { $group: { _id: null, avg: { $avg: '$calificacion' }, count: { $sum: 1 } } },
            ]).session(session);
            if (stats) {
                await this.restauranteModel.findByIdAndUpdate(data.restaurante_id, {
                    $set: {
                        calificacion_prom: Math.round(stats.avg * 10) / 10,
                        total_resenas: stats.count,
                    },
                }, { session });
            }
        }

        if (data.orden_id) {
            await this.ordenModel.findByIdAndUpdate(data.orden_id, {
                $set: { tiene_resena: true },
            }, { session });
        }

        await session.commitTransaction();
        return resena;
    } catch (err) {
        await session.abortTransaction();
        throw err;
    } finally {
        await session.endSession();
    }
}
```

**Tests a actualizar:** `apps/api/src/resenas/resenas.service.spec.ts`

- Mock de `connection.startSession()` (patrón ya establecido en ordenes/restaurantes specs)
- Verificar que `create()` recibe `[data], { session }` (array syntax para transacciones)
- Verificar `commitTransaction` en happy path, `abortTransaction` en error

---

### BUG-02 — `usuarios.create()` normalizar email a minúsculas

**Archivo:** `apps/api/src/usuarios/usuarios.service.ts`

**Cambio:**

```typescript
async create(data: any): Promise<UsuarioDocument> {
    if (data.email) {
        data.email = data.email.toLowerCase().trim();
    }
    return this.usuarioModel.create(data);
}
```

**Tests a actualizar:** `apps/api/src/usuarios/usuarios.service.spec.ts`

- Añadir test: `create() normaliza email a minúsculas`
- Verificar que `create()` es llamado con email en minúsculas

---

### BUG-03 — `resenas.remove()` actualizar campos desnormalizados

**Archivo:** `apps/api/src/resenas/resenas.service.ts`

**Cambio:**

```typescript
async remove(id: string): Promise<{ deleted: boolean }> {
    const resena = await this.resenaModel.findByIdAndDelete(id).exec();
    if (!resena) throw new NotFoundException('Reseña no encontrada');

    // Recalcular calificacion_prom y total_resenas del restaurante
    if (resena.restaurante_id) {
        const [stats] = await this.resenaModel.aggregate([
            { $match: { restaurante_id: resena.restaurante_id, activa: true } },
            { $group: { _id: null, avg: { $avg: '$calificacion' }, count: { $sum: 1 } } },
        ]);
        await this.restauranteModel.findByIdAndUpdate(resena.restaurante_id, {
            $set: {
                calificacion_prom: stats ? Math.round(stats.avg * 10) / 10 : 0,
                total_resenas: stats?.count ?? 0,
            },
        });
    }

    // Resetear tiene_resena en la orden
    if (resena.orden_id) {
        await this.ordenModel.findByIdAndUpdate(resena.orden_id, {
            $set: { tiene_resena: false },
        });
    }

    return { deleted: true };
}
```

**Tests a actualizar:** `apps/api/src/resenas/resenas.service.spec.ts`

- Verificar que `remove()` recalcula `calificacion_prom`/`total_resenas`
- Verificar que `remove()` resetea `tiene_resena: false`

---

### BUG-04 — `ordenes.remove()/removeMany()` decrementar `veces_ordenado`

**Archivo:** `apps/api/src/ordenes/ordenes.service.ts`

**Cambio en `remove()`:**

```typescript
async remove(id: string): Promise<{ deleted: boolean }> {
    const orden = await this.ordenModel.findByIdAndDelete(id).exec();
    if (!orden) throw new NotFoundException('Orden no encontrada');

    // Decrementar veces_ordenado en cada menu_item
    const bulkOps = orden.items.map((item: any) => ({
        updateOne: {
            filter: { _id: item.item_id || item.menu_item_id },
            update: { $inc: { veces_ordenado: -(item.cantidad || 0) } },
        },
    }));
    if (bulkOps.length) {
        await this.menuItemModel.bulkWrite(bulkOps as any[]);
    }

    return { deleted: true };
}
```

**Cambio en `removeMany()`:** Análogo pero con `find()` previo para obtener items.

**Tests a actualizar:** `apps/api/src/ordenes/ordenes.service.spec.ts`

- Verificar que `remove()` ejecuta `bulkWrite` con `$inc` negativo
- Verificar que `removeMany()` decrementa para todas las órdenes eliminadas

---

### BUG-05 — `ordenes.updateStatus()` validar transiciones legales

**Archivo:** `apps/api/src/ordenes/ordenes.service.ts`

**Cambio — agregar mapa de transiciones:**

```typescript
const TRANSICIONES: Record<string, string[]> = {
  pendiente: ["en_proceso", "cancelado"],
  en_proceso: ["en_camino", "cancelado"],
  en_camino: ["entregado", "cancelado"],
  entregado: [],
  cancelado: [],
};
```

En `updateStatus()`, leer el estado actual y validar:

```typescript
const orden = await this.ordenModel.findById(id).select("estado").lean().exec();
if (!orden) throw new NotFoundException("Orden no encontrada");
const permitidos = TRANSICIONES[orden.estado] ?? [];
if (!permitidos.includes(estado)) {
  throw new BadRequestException(
    `Transición inválida: ${orden.estado} → ${estado}. Permitidos: ${permitidos.join(", ") || "ninguno (estado terminal)"}`,
  );
}
```

**Tests a actualizar:** `apps/api/src/ordenes/ordenes.service.spec.ts`

- Añadir tests para transiciones válidas e inválidas
- Verificar mensaje de error descriptivo

---

### BUG-06 — `restaurantes.remove()` agregar cascada o proteger

**Archivo:** `apps/api/src/restaurantes/restaurantes.service.ts`

**Opción A (recomendada): Proteger `remove()` — solo permitir si no tiene datos asociados:**

```typescript
async remove(id: string): Promise<{ deleted: boolean }> {
    const [ordenesCount, menuItemsCount] = await Promise.all([
        this.ordenModel.countDocuments({ restaurante_id: id }),
        this.menuItemModel.countDocuments({ restaurante_id: id }),
    ]);
    if (ordenesCount > 0 || menuItemsCount > 0) {
        throw new BadRequestException(
            'No se puede eliminar: el restaurante tiene datos asociados. Usa PATCH :id/cancel en su lugar.',
        );
    }
    const result = await this.restauranteModel.findByIdAndDelete(id).exec();
    if (!result) throw new NotFoundException('Restaurante no encontrado');
    return { deleted: true };
}
```

**Tests a actualizar:** `apps/api/src/restaurantes/restaurantes.service.spec.ts`

- Verificar que `remove()` lanza error si hay datos asociados
- Verificar que `remove()` funciona si no hay datos asociados

---

### BUG-07 — `usuarios.findAll()` escapar regex de email

**Archivo:** `apps/api/src/usuarios/usuarios.service.ts`

**Cambio:**

```typescript
if (query.email) {
  const escaped = query.email.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
  filter.email = new RegExp(escaped, "i");
}
```

**Tests a actualizar:** `apps/api/src/usuarios/usuarios.service.spec.ts`

- Verificar que caracteres especiales en email son escapados

---

## Orden de ejecución recomendado

1. **BUG-02**: Normalizar email en create() — cambio de 1 línea, bajo riesgo
2. **BUG-07**: Escapar regex de email — cambio de 1 línea, bajo riesgo
3. **BUG-01**: Transacción ACID en resenas.create() — patrón ya existente en ordenes/restaurantes
4. **BUG-03**: Actualizar desnormalizados en resenas.remove() — depende del patrón de BUG-01
5. **BUG-05**: Validar transiciones de estado en updateStatus() — lógica nueva
6. **BUG-04**: Decrementar veces_ordenado en remove()/removeMany() — requiere leer orden antes de borrar
7. **BUG-06**: Proteger restaurantes.remove() — validación adicional
8. Ejecutar `npm test` — esperar ≥212 tests ✓ (se añadirán tests nuevos)
9. Commit: `fix: transacción en resenas, email normalization, state transitions, cascada`

---

## Estado objetivo

- ≥212 tests ✓ (nuevos tests para cada bug corregido)
- Todas las operaciones multi-documento usan transacciones ACID
- Email normalizado a lowercase en creación y login
- Campos desnormalizados actualizados consistentemente en create() y remove()
- Transiciones de estado validadas con mapa de estados
- `remove()` de restaurante protegido contra datos huérfanos
- Input de regex sanitizado contra caracteres especiales
