# Roadmap de Fixes — Audit #8 (2026-03-10f)
**Tests actuales:** 197 passed / 11 suites

---

## FIX-01 — Añadir índice multikey en `usuarios.preferencias`

**Prioridad:** HIGH
**Archivos:**
- `apps/api/src/usuarios/schemas/usuario.schema.ts`
- `apps/database/operations/indexes.js`

**Cambio en schema:**
```typescript
// Al final de usuario.schema.ts:
UsuarioSchema.index({ preferencias: 1 }, { name: 'idx_usuarios_preferencias' });
```

**Cambio en indexes.js:**
```javascript
// Añadir en createUserIndexes():
col.createIndex({ preferencias: 1 }, { name: 'preferencias_multikey' }),
```

**Crear en live DB:**
```javascript
db.usuarios.createIndex({ preferencias: 1 }, { name: 'preferencias_multikey' })
```

---

## FIX-02 — Añadir índice multikey en `resenas.likes`

**Prioridad:** HIGH
**Archivos:**
- `apps/api/src/resenas/schemas/resena.schema.ts`
- `apps/database/operations/indexes.js`

**Cambio en schema:**
```typescript
// Al final de resena.schema.ts:
ResenaSchema.index({ likes: 1 }, { name: 'idx_resenas_likes' });
```

**Cambio en indexes.js:**
```javascript
// Añadir en createReviewIndexes():
col.createIndex({ likes: 1 }, { name: 'likes_multikey' }),
```

**Crear en live DB:**
```javascript
db.resenas.createIndex({ likes: 1 }, { name: 'likes_multikey' })
```

---

## Checklist rúbrica final (post Audit #8)

| Criterio                                              | Estado |
|-------------------------------------------------------|--------|
| Transacción Crear Orden (ACID + check disponible + dedup) | ✅  |
| Transacción Cancelar Restaurante (ACID)               | ✅     |
| Top restaurantes con reseñas activas (activa:true)    | ✅     |
| Best sellers ($unwind + $group + $toDouble Decimal128) | ✅    |
| Revenue by day (rango 2023-2025)                      | ✅     |
| Revenue by restaurant/month (zero-padded)             | ✅     |
| Top spenders ($lookup + exclusión password)           | ✅     |
| Desnorm (calificacion_prom, tiene_resena, veces_ordenado) | ✅  |
| GridFS upload/download/delete                         | ✅     |
| Índice multikey usuarios.preferencias                 | ⚠️ FIX-01 |
| Índice multikey resenas.likes                         | ⚠️ FIX-02 |
| Seed 50k docs + reseñas + ratings                     | ✅     |
| Tests: 197+ passed                                    | ✅     |
