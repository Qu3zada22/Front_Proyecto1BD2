# Roadmap — Post Auditoría #18

**Fecha:** 2026-03-09
**Estado actual:** 241 tests passing | 1 bug + 4 observaciones pendientes

---

## Prioridad Alta (afecta demo)

### 1. BUG-01: `verify.js` — `.toFixed(2)` sobre Decimal128 → crash

**Archivo:** `apps/database/verify.js` (líneas 142, 164)

El script `npm run verify` crashea en la sección de pipelines con `TypeError: r.total_revenue.toFixed is not a function`. Las secciones de arrays y GridFS no se ejecutan.

- [ ] Línea 142: Reemplazar acceso directo a `.toFixed()` por `parseFloat(val.toString()).toFixed(2)`:
  ```javascript
  const rev = parseFloat(r.total_revenue.toString());
  const tick = parseFloat(r.ticket_prom.toString());
  // usar rev.toFixed(2) y tick.toFixed(2) en el console.log
  ```
- [ ] Línea 164: Igual para `r.total_gastado`:
  ```javascript
  const gastado = parseFloat(r.total_gastado.toString());
  // usar gastado.toFixed(2) en el console.log
  ```
- [ ] Verificar que `npm run verify` completa sin errores

**Estimación:** ~6 líneas

---

## Prioridad Media

### 2. OBS-01: `create-orden.dto.ts` — `precio` y `nombre` opcionales

**Archivo:** `apps/api/src/ordenes/dto/create-orden.dto.ts`

- [ ] Agregar `@IsOptional()` a `precio` en `ItemOrdenDto`
- [ ] Agregar `@IsOptional()` a `nombre` en `ItemOrdenDto`
- [ ] Actualizar `@ApiProperty` → `@ApiPropertyOptional` con descripción "Ignorado — se lee de BD"
- [ ] Verificar que los tests de create siguen pasando (los mocks no envían precio pero el mock de find sí retorna precio)

**Estimación:** ~4 líneas

### 3. OBS-02: `menu-items.service.ts` — Verificar `activo: true` al crear item

**Archivos:** `menu-items.service.ts`, `menu-items.service.spec.ts`

- [ ] Cambiar `countDocuments({ _id: dto.restaurante_id })` → `countDocuments({ _id: dto.restaurante_id, activo: true })`
- [ ] Cambiar error message: `'El restaurante referenciado no existe'` → `'El restaurante referenciado no existe o está inactivo'`
- [ ] Actualizar test de FK: cambiar mock a `mockRestauraunteModel.countDocuments.mockResolvedValue(0)` y verificar nuevo mensaje
- [ ] Agregar test: crear item para restaurante inactivo → BadRequestException

**Estimación:** ~3 líneas de código + ~10 de tests

---

## Prioridad Baja

### 4. OBS-03: `restaurantesPorCategoria` — Renombrar `_id` a `categoria`

**Archivos:** `reportes.service.ts`, `reportes.service.spec.ts`

- [ ] Agregar `{ $project: { categoria: '$_id', total: 1, _id: 0 } }` en `restaurantesPorCategoria()`
- [ ] Actualizar test: verificar que pipeline tiene `$project` con `categoria: '$_id'`
- [ ] (Opcional) Actualizar expected value en el test a `{ categoria: 'italiana', total: 5 }` format

**Estimación:** ~2 líneas de código + ~5 de tests

### 5. OBS-04: `usuarios.controller.ts` — NaN guard en `parseInt`

**Archivo:** `apps/api/src/usuarios/usuarios.controller.ts`

- [ ] Cambiar `parseInt(skip, 10)` → `parseInt(skip, 10) || 0`
- [ ] Cambiar `parseInt(limit, 10)` → `parseInt(limit, 10) || 50`

**Estimación:** ~2 líneas

---

## Orden de Ejecución Recomendado

```
1. BUG-01 — verify.js Decimal128.toFixed (crítico para demo, 2 líneas)
2. OBS-02 — menu-items activo:true + tests (combinar con BUG-01 en mismo commit)
3. OBS-03 — restaurantesPorCategoria $project (independiente, mínimo impacto)
4. OBS-01 — precio/nombre opcionales en DTO (cosmético)
5. OBS-04 — parseInt NaN guard (cosmético)
6. Correr tests — npx jest --no-cache (verificar ≥244 tests)
7. Probar npm run verify manualmente con datos del seed
```

---

## Métricas Esperadas Post-Fix

| Métrica                | Antes    | Después (esperado) |
| ---------------------- | -------- | ------------------ |
| Tests                  | 241      | ~244               |
| Suites                 | 11       | 11                 |
| Bugs abiertos          | 1        | 0                  |
| Observaciones abiertas | 4        | 0                  |
| `npm run verify`       | ❌ crash | ✅ completa        |
| PDF spec compliance    | 100%     | 100%               |

---

## Checklist Final Pre-Entrega

- [ ] `POST /api/seed` completa sin errores (50k órdenes)
- [ ] `POST /api/orders` con datos seeded → total correcto (no null/NaN)
- [ ] `GET /api/reports/revenue/by-day` → devuelve ingresos
- [ ] `GET /api/reports/restaurants/top-rated` → devuelve restaurantes
- [ ] `GET /api/reports/menu-items/best-sellers` → devuelve items
- [ ] `POST /api/files/upload` + `GET /api/files/:id` → funciona
- [ ] `DELETE /api/seed` limpia toda la DB
- [ ] `npx jest --no-cache` → 0 failures
- [ ] `npm run verify` → completa sin errores (después de BUG-01 fix)
- [ ] API responde en `http://localhost:3000/api`
- [ ] Swagger disponible en `http://localhost:3000/docs`
- [ ] Al menos 4 tipos de índices visibles en explain() output de verify.js
